{
  "name": "explain-regexp",
  "version": "0.1.0",
  "type": "module",
  "bin": "./bin/index.js",
  "scripts": {
    "prepublishOnly": "npm run build",
    "watch": "vite build --mode development --watch",
    "build": "vite build",
    "lint": "eslint src/*/**.ts",
    "test": "./bin/index.js '/^((?=\\S*?[A-Z])(?=\\S*?[a-z])(?=\\S*?[0-9]).{6,})\\S$/g'"
  },
  "dependencies": {
    "ecma-262-regexp-parser": "0.1.0",
    "commander": "^10.0.0"
  }
}
